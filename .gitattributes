# Enforce LF for shell scripts
*.sh text eol=lf

# Normalize line endings for all text files
* text=auto

# Prevent CRLF for config and Docker files
*.yml text eol=lf
*.yaml text eol=lf
Dockerfile* text eol=lf