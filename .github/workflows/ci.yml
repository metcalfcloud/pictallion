name: 🔄 CI/CD

on:
  push:
    branches: [main, develop]
    tags-ignore:
      - 'v*.*.*'
  pull_request:
    branches: [main, develop]

jobs:
  test:
    name: 🧪 Test & Lint
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: pictallion_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 📦 Install mise
        run: |
          curl -fsSL https://mise.jdx.dev/install.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          ~/.local/bin/mise --version

      - name: 🔐 Trust repo tasks & install toolchains
        run: |
          mise trust .
          mise install

      - name: 🧰 Install Linux native dependencies (GTK/WebKit)
        run: |
          sudo apt-get update -y
          sudo apt-get install -y build-essential pkg-config libssl-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev libwebkit2gtk-4.1-dev || sudo apt-get install -y libwebkit2gtk-4.0-dev

      - name: 🎨 Lint (frontend + backend)
        run: |
          mise run lint

      - name: 🏗️ Build frontend
        run: |
          mise run build-frontend

      # Type checking happens during build in frontend script

      # Install Playwright browsers for e2e (Chromium only for speed)
      - name: 🎭 Install Playwright browsers (Chromium only)
        run: |
          npx playwright install chromium

      - name: 🧪 Run unit tests (Vitest)
        run: |
          mise run test-frontend
      - name: Upload client coverage to Codecov
        if: ${{ env.ACT != 'true' && hashFiles('frontend/coverage/lcov.info') != '' }}
        uses: codecov/codecov-action@v3
        with:
          file: frontend/coverage/lcov.info
          flags: tauri-frontend
          name: tauri-frontend-coverage

      - name: 🎭 Run E2E smoke (Chromium)
        run: |
          mise run e2e-smoke-chromium

      - name: 💾 Cache cargo registry and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/src-tauri/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: 🏗️ Build Rust/Tauri backend
        run: |
          mise run build-backend

      - name:  Run Rust unit tests
        run: |
          mise run test-backend

      - name: 🧪 Run server tests (placeholder)
        run: |
          echo "Server tests are managed in test-python-backend.yml"
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/pictallion_test

  build:
    name: 🏗️ Build Check
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 📦 Install mise
        run: |
          curl -fsSL https://mise.jdx.dev/install.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          ~/.local/bin/mise --version

      - name: 🔐 Trust repo tasks & install toolchains
        run: |
          mise trust .
          mise install

      - name: 🧰 Install Linux native dependencies (GTK/WebKit)
        run: |
          sudo apt-get update -y
          sudo apt-get install -y build-essential pkg-config libssl-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev libwebkit2gtk-4.1-dev || sudo apt-get install -y libwebkit2gtk-4.0-dev

      - name: 🏗️ Build application (Vite + Tauri)
        run: |
          mise run build

      - name: 📦 Test Docker build
        run: |
          docker build -f docker/Dockerfile -t pictallion:test .

      - name: 🔍 Scan Docker image with Trivy
        uses: aquasecurity/trivy-action@v0.13.1
        with:
          image-ref: pictallion:test
          format: table
          exit-code: 1
          ignore-unfixed: true
          vuln-type: 'os,library'

  security:
    name: 🔒 Basic Security Check
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 📦 Install mise
        run: |
          curl -fsSL https://mise.jdx.dev/install.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          ~/.local/bin/mise --version

      - name: 🔐 Trust repo tasks & install toolchains
        run: |
          mise trust .
          mise install

      - name: 🔧 Install dependencies (frontend)
        run: |
          cd frontend
          npm ci

      - name: 🔍 Run security audit (npm)
        run: |
          cd frontend
          # Run audit and capture output
          npm audit --audit-level=high --json > audit-results.json || true

          # Check for high or critical vulnerabilities only
          HIGH_CRITICAL=$(cat audit-results.json | jq -r '.vulnerabilities | to_entries[] | select(.value.severity == "high" or .value.severity == "critical") | .key' | wc -l)

          if [ "$HIGH_CRITICAL" -gt "0" ]; then
            echo "❌ Found high or critical vulnerabilities:"
            cat audit-results.json | jq -r '.vulnerabilities | to_entries[] | select(.value.severity == "high" or .value.severity == "critical") | "\(.key): \(.value.severity)"'
            exit 1
          else
            echo "✅ No high or critical vulnerabilities found"
            # Show total vulnerability count but don't fail
            TOTAL=$(cat audit-results.json | jq -r '.vulnerabilities | length')
            if [ "$TOTAL" -gt "0" ]; then
              echo "ℹ️ Found $TOTAL total vulnerabilities (see weekly security report for details)"
            fi
          fi
